# Infra

This directory contains runtime and deployment infrastructure assets used by AppLens.

## Infrastructure Scope

- compose configurations for local and deployed environments
- reverse-proxy and routing configuration
- environment templates and runtime defaults
- container-related support files
- deployment references and integration points
